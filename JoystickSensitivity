int joystickSensitivityFunction(int joystickValue)
{
//Variables to make it so that it becomes a cube function. 
int newValue = 0;
int maxRange = 100;
int maxMotorPower = 127;

newValue = ( joystickValue^3 ) / ( maxRange^3 ) * maxMotorPower
//What it comes back with
return(newValue);
}
